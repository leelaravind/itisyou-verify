{
  "name": "@verify/app",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production"
  },
  "dependencies": {
    "@verify/connectors": "workspace:*",
    "@verify/contracts": "workspace:*",
    "@verify/domain": "workspace:*",
    "@verify/security": "workspace:*",
    "@verify/ui": "workspace:*",
    "hono": "^4.13.8"
  }
}
