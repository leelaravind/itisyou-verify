What is verified about the two owner-blocked items, and what is left
21 September 2026, against production at c2972fb85489.

The point of this file is to shrink both items to the smallest possible action, and to be
exact about which part genuinely cannot be done by anyone but the owner.


=== B9. The owner panel, signed in ===

Everything around the sign-in is verified. The sign-in itself is not, and cannot be.

  /admin/login on production                    200, serves the form
  platform owners in the production database    1
  of those with TOTP enrolled                   1
  auth.totp.enrolled (production audit)         1, 2026-09-20 11:09Z
  auth.totp.accepted (production audit)         1, 2026-09-20 11:37Z
  auth.signin (production audit)                5, latest 2026-09-21 12:30Z
  sign_in_link mail delivered from production   11 rows, every one state=sent,
                                                latest 2026-09-21 12:29Z
  anonymous /owner                              404
  anonymous /app                                401
  the panel rendered at three widths            13 screens, no overflow, exclusion-clean

So: the account exists, the second factor is enrolled and has been accepted on production
before, the mail that carries the link is being delivered from production, and the panel
renders. What has never happened is a person opening that link and typing the code, and
nobody may do it for them. A production session must not be seeded and authentication must
not be bypassed, which is the owner's own instruction and the right one.

THE ACTION, in full:
  1. Open https://verify.itisyou.app/admin/login
  2. Type the owner address, press "Email me a link"
  3. Open the link from the mailbox
  4. When prompted, enter the six-digit code from the authenticator app
  5. Say so, and the panel gets walked against production data in this session


=== B10. support@itisyou.app ===

DNS is verified and the routing rule is not, because it cannot be read from here.

  MX      route1/2/3.mx.cloudflare.net    (Cloudflare Email Routing)
  SPF     v=spf1 include:_spf.mx.cloudflare.net ~all

Whether a rule ACCEPTS support@ is observable from outside, by offering the recipient over
SMTP without sending a message. That was attempted, and the exact refusal is the finding:

  connected to route1.mx.cloudflare.net:25
  <= 550 Sender IP reverse lookup rejected (2a01:4b00:...)

Cloudflare refuses the session before any recipient can be offered, because this machine is
on a connection with no reverse DNS. That is a fact about where the probe runs, not about the
routing rule, and no amount of retrying changes it. A host with reverse DNS could answer the
question; this one cannot.

THE ACTION, in full:
  1. Cloudflare dashboard, itisyou.app, Email, Email Routing, Routes
  2. Confirm a rule whose custom address is support@itisyou.app and whose destination is a
     mailbox you actually read
  3. Send one message to support@itisyou.app from any account and confirm it arrives

Replying AS support@itisyou.app is a separate thing and needs an outbound identity: either
that address verified as a sending identity in Resend, or a "send as" identity in the mail
client. Cloudflare Email Routing is receive-only and cannot send.


=== A thing worth noting while reading production ===

The owner alert channel is live right now. notification_deliveries carries 19
milestone_reached rows, every one state=sent, the most recent at 2026-09-21 17:36Z, which is
the release notification for this afternoon's deploy. The path that would carry a payment
alert is therefore delivering today, not merely correct in the code.


=== re-checked at 19:19Z, against production 9563df87e6fe ===

  200  /admin/login, and the page really offers "Email me a link" (1 occurrence)
  404  /owner        anonymous
  401  /app          anonymous
  /health            9563df87e6fe5ec05e0bc309e08372cf4e6aff37

  Status text for the two items, read from docs/completion-checklist.md:
    B9   "blocked on the owner, one action"
    B10  "blocked on the owner, one dashboard check"

  Nothing in the documents records either as done. The evidence columns describe what is
  verified AROUND them, which is a different claim and is the accurate one.

  An audit of these claims was started on a cheap model and hung for half an hour without
  reaching its first check, almost certainly on the production database command it had been
  warned might be refused. It was stopped rather than left running, the mechanical checks
  above were run directly, and a tighter read-only agent was given the one question that
  matters: whether any document overclaims B9 or B10.

  That agent returned in 39 seconds with six tool calls: OVERCLAIMS, none. It quoted both
  status strings back, noted that B10's acceptance criterion reads "Rule exists" while its
  evidence column explicitly says the rule cannot be read from here, and judged section E of
  the completion checklist specific enough to act on without a follow-up question.

  The contrast is the useful part: the same question, asked with file reads only and a hard
  call budget, cost 39 seconds. Asked with a database command in it, it cost half an hour and
  returned nothing.


=== 22 September: what the owner reported, and what follows from it ===

The owner signed in to the panel and was not asked for a six-digit code, and separately sent
a test message to support@itisyou.app and received it at the configured destination.

The missing code is correct. owner.view is the only read-only capability, so reading the
panel needs a session and the platform-owner flag; every write, refund, switch and approval
is consequential and needs a two-factor check inside fifteen minutes. OWNER-930 proves it
against the D1 port: mfa_verified_at NULL reads /owner at 200 and is refused 403 on a write,
and removing the gate makes that write answer 422 instead.

Receipt at support@ is closed by the owner's own test, which is the only way it could be
closed.

On replying AS support@itisyou.app, one inference is now available that was not before.
Production holds both RESEND_API_KEY and RESEND_FROM_ADDRESS as secrets, and has delivered 11
sign_in_link emails, every one state=sent. Resend refuses to send from an unverified domain,
so the domain of that from-address IS verified in Resend. Resend verifies DOMAINS rather than
individual addresses.

So if RESEND_FROM_ADDRESS is on itisyou.app, sending as support@itisyou.app needs no new
verification at all, and the remaining work is a choice rather than a setup task. The secret's
value is not read here, and the owner can settle it in one second: look at the From line of
any sign-in email they have received. If it ends @itisyou.app, nothing further is needed.
